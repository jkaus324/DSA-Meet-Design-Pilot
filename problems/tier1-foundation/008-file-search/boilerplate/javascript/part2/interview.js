// Data class (given).

// TODO: design and implement your solution.
// Required functions:
//   function reset_service()
//   function fs_build_default_tree()
//   function fs_build_empty_tree()
//   function fs_build_single_file_tree()
//   function fs_count_by_extension(ext)
//   function fs_has_by_extension(ext, name)
//   function fs_count_by_size(minSize)
//   function fs_has_by_size(minSize, name)
//   function fs_count_by_name(sub)
//   function fs_has_by_name(sub, name)
//   function fs_count_composite_and(ext, minSize)
//   function fs_count_composite_or(ext, minSize)

function reset_service() {
  // TODO: write your solution
  return null;
}

function fs_build_default_tree() {
  // TODO: write your solution
  return null;
}

function fs_build_empty_tree() {
  // TODO: write your solution
  return null;
}

function fs_build_single_file_tree() {
  // TODO: write your solution
  return null;
}

function fs_count_by_extension(ext) {
  // TODO: write your solution
  return null;
}

function fs_has_by_extension(ext, name) {
  // TODO: write your solution
  return null;
}

function fs_count_by_size(minSize) {
  // TODO: write your solution
  return null;
}

function fs_has_by_size(minSize, name) {
  // TODO: write your solution
  return null;
}

function fs_count_by_name(sub) {
  // TODO: write your solution
  return null;
}

function fs_has_by_name(sub, name) {
  // TODO: write your solution
  return null;
}

function fs_count_composite_and(ext, minSize) {
  // TODO: write your solution
  return null;
}

function fs_count_composite_or(ext, minSize) {
  // TODO: write your solution
  return null;
}

// ── Export everything the test runner needs (do not remove) ──
// If you add classes (e.g. strategy subclasses), add them here too.
module.exports = { reset_service, fs_build_default_tree, fs_build_empty_tree, fs_build_single_file_tree, fs_count_by_extension, fs_has_by_extension, fs_count_by_size, fs_has_by_size, fs_count_by_name, fs_has_by_name, fs_count_composite_and, fs_count_composite_or };
